[package]
name = "tranquility-types"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1.0.136", features = ["derive"] }
serde_json = "1.0.79"
time = { version = "0.3.9", optional = true, features = ["serde", "serde-well-known"] }

[features]
activitypub = ["time"]
mastodon = ["time"]
nodeinfo = [ ]
webfinger = [ ]
