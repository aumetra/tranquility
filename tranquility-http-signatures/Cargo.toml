[package]
name = "tranquility-http-signatures"
version = "0.1.0"
authors = ["Glitch <smallglitch@cryptolab.net>"]
edition = "2018"
license = "MIT"

[dependencies]
base64 = "0.13.0"
http = "0.2.5"
once_cell = "1.8.0"
pem = "1.0.0"
pkcs8 = { version = "0.7.6", features = ["alloc"] }
ring = { version = "0.16.20", features = ["std"] }
thiserror = "1.0.30"

[dependencies.reqwest]
version = "0.11.6"
default-features = false
optional = true

[features]
default = []
